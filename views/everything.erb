<!DOCTYPE html>
<html>
  <head>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container">
    <h1> Task Manager </h1>
    <h3> These are your To Do Lists... </h3>
      <table class="table table-striped table-hover table-bordered">
        <thead>
          <tr>
          <th> Lists </th>
          </tr>
        </thead>
        <tbody>
          <% @lists.each do |list| %>
            <tr> 
              <td> <a href="/list/<%=list.name%>"><%= list.name %></a> </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <h3> These are all your tasks: </h3>
    <table class="table table-striped table-hover table-bordered">
      <thead>
        <tr>
        <th> Description </th>
        <th> Due Date </th>
        <th> Completion Status </th>
        </tr>
      </thead>
      <tbody>
        <% @items.each do |item| %>
            <tr> 
              <td> <%= item.description %> </td>
              <td> <%= item.due_date %> </td>
              <td> <%= item.done %> </td>
            </tr>
          <% end %>
      </tbody>
    </table>
    </div>
  </body>
</html>