<!DOCTYPE html>
<html>
  <head>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container">
    <h1> <%= @list_name["name"] %> </h1>
    <table class="table table-striped table-hover table-bordered">
      <thead>
        <tr>
        <th> Description </th>
        <th> Due Date </th>
        <th> Completion Status </th>
        </tr>
      </thead>
      <tbody>
        <% @specific_list.each do |task| %>
            <tr> 
              <td> <%= task.description %> </td>
              <td> <%= task.due_date %> </td>
              <td> <%= task.done %> </td>
                <% if task.done == true %>
                  <td><input type="checkbox" id="blankCheckbox" value="option1" checked aria-label="..."></td>
                <% else %>
                  <td><input type="checkbox" id="blankCheckbox" value="option1" aria-label="..."></td>
                <% end %>
            </tr>
          <% end %>
      </tbody>
    </table>
    </div>
  </body>
</html>